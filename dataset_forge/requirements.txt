# Dataset Forge dependencies — kept separate from the installable picoface
# package so they never leak into `pip install picoface`. Install picoface
# itself first (`pip install -e .` from the repo root): the Forge uses its
# `load_dataset()` to validate exports and its models for the smoke check.
#
# Pinned exactly: an export is only byte-for-byte reproducible with the same
# rasterizer and random-number versions (manifest.json records both).
Pillow==12.3.0
numpy==2.5.2
