## 1. `pyproject.toml` publish-readiness

- [x] 1.1 Add PyPI-facing `classifiers` (supported Python versions, license, intended audience/topic) and `keywords` to `[project]` in `pyproject.toml`. Verify by running `python -c "import tomllib, pprint; pprint.pprint(tomllib.load(open('pyproject.toml','rb'))['project'])"` and confirming the new fields are present and well-formed.
- [x] 1.2 Add `build` and `twine` to the `dev` optional-dependencies group. Verify by running `pip install -e .[dev]` and confirming `python -m build --version` and `twine --version` both succeed.
- [x] 1.3 Run `python -m build` from the repo root and confirm it produces a clean sdist and wheel with no errors or warnings that indicate missing metadata. Verify by checking `dist/*.whl` and `dist/*.tar.gz` exist and `twine check dist/*` reports both as passing.

## 2. `RELEASING.md`

- [x] 2.1 Write `RELEASING.md` at the repo root: verifying the `picoface` name is actually free on PyPI (first step, before anything else); creating a PyPI account and an API token (token-based, not password); a recommended TestPyPI dry run; running `python -m build`; running `twine upload dist/*`; the versioning/tagging convention for future releases; and an explicit note that `pip install picoface` will not force the CPU-only torch wheel — the two-step install order README already documents remains necessary regardless of channel. Verify by re-reading it against this list and confirming every item is covered and the commands referenced match the `dev` extras added in task 1.2.

## 3. `README.md` rewrite

- [x] 3.1 Replace the stale "Status: early scaffolding (Phase 0)" line with an accurate summary of the current state (Phases 0-6 implemented; Phase 7 docs/packaging-prep in progress; real-dataset distribution and template notebooks explicitly out of this MVP). Verify by reading it back against `openspec/ROADMAP.md`'s phase table for accuracy.
- [x] 3.2 Add an explicit constraints section: CPU-only always, no GPU required or used, training completes in seconds to minutes on an old laptop. Verify by cross-checking the wording against `openspec/ROADMAP.md`'s Binding Constraint section for consistency.
- [x] 3.3 Add a quickstart code snippet covering the walked-through student API (`load_dataset`, `build_classifier`/`build_vae`, `train`, `evaluate`, `predict`, `generate`, `classify_generated`, `activation_maximize`), noting `build_autoencoder()` and `build_classifier_from_shape()` exist but aren't part of this walkthrough. Verify by actually running the snippet's calls (adapted against `picoface._internals.stub_data.make_stub_dataset` or an equivalent small dataset in a scratch script, not committed) to confirm every function name, argument, and return usage shown is correct.
- [x] 3.4 Add a note on `load_dataset()`'s data-contract expectation (`.npz` + `classes.json`) that's explicit about picoface not providing a real dataset itself. Verify by reading it back and confirming it states the contract as fact without implying picoface solves distribution.
- [x] 3.5 Add a "what to expect" results section with documented, non-gating ranges (CNN held-out ~0.98-1.00, VAE held-out ~0.95-1.00, reconstruction ~0.8-1.0 class-dependent, `classify_generated()` overall ~0.81) sourced from `openspec/changes/archive/2026-09-23-picoface-phase6/diagnostics.md` and `openspec/changes/archive/capstone-linkage-sampling-fix/diagnostics.md` (or its current unarchived path if not yet archived). Verify by cross-checking every number against its source diagnostics file and confirming the section states plainly that nothing in the repo enforces these numbers.
- [x] 3.6 Add a note that template notebooks per arm plus a capstone notebook are a planned nice-to-have, not shipped in this MVP, and update the `Layout` section so it doesn't imply notebooks exist. Verify by re-reading the `Layout` section against the actual repo tree (`find . -iname "*.ipynb"` returns nothing).
- [x] 3.7 Update the install section: keep the current git-clone-or-zip + `pip install -e .` instructions (including the CPU-torch-wheel-first ordering) as what works today, and add a note that a PyPI release is planned as the primary long-term channel, pointing to `RELEASING.md`. Verify by confirming the existing install commands are unchanged (still accurate) and the new note doesn't imply `pip install picoface` works yet.

## 4. `openspec/ROADMAP.md` updates

- [x] 4.1 Revise the Non-Goals line "Publishing to PyPI or finalizing the distribution channel — packaging structure is in scope, the release process is not" to distinguish *preparing* for a release (now in scope, done by this change) from *performing* the actual upload (still the user's own manual action, not this repo's automation). Verify by re-reading the revised line alongside `RELEASING.md` and confirming they don't contradict each other.
- [x] 4.2 Resolve the "Final distribution channel" Open Question entry: PyPI is the chosen target, with git-clone/zip + `pip install -e .` remaining accurate as what works today, following the same strikethrough-and-resolve pattern already used for other resolved entries in that section. Verify the pattern matches neighboring resolved entries.
- [x] 4.3 Resolve the accuracy/quality-bar Open Question entry as documented-ranges-only (no automated gate), referencing README's new "what to expect" section, using the same strikethrough-and-resolve pattern. Verify the pattern matches neighboring resolved entries.
- [x] 4.4 Update the two notebook-content Open Questions entries (`build_autoencoder()` walkthrough; `viz` gaining `GeneratedImagesReport`/`activation_maximize()` plotting helpers) from "resolved in Phase 7" to explicitly deferred/non-blocking, without implying a decision was made against notebooks. Verify by re-reading both entries and confirming neither claims a resolution that didn't happen.
- [x] 4.5 Update the Phase 7 phase-table row to reflect the real scope (docs + PyPI-publish-prep done; template notebooks and real-dataset distribution explicitly out of this MVP) and mark it done-as-scoped, following the same phrasing style used for other completed phase-table rows. Verify by comparing the row's phrasing against other completed rows for consistency.

## 5. Full verification pass

- [x] 5.1 Run the full project test suite and confirm nothing regressed from the `pyproject.toml` metadata changes. Verify with `pytest` passing exactly as it did before this change.
- [x] 5.2 Confirm the base install command README documents (`pip install -e .`, no extras) still succeeds cleanly after the `pyproject.toml` edits. Verify by running it in a clean virtual environment and confirming `import picoface` succeeds afterward.
