# Dataset Forge

Tooling for building and curating datasets used by `picoface`. This directory
is intentionally kept outside the installable `picoface` package (see
`../openspec/ROADMAP.md`) so its dependencies never leak into a student's
`pip install picoface`.

Not yet implemented — see the project roadmap for phasing.
