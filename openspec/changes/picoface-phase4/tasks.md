## 1. model-interface: `latent_access` capability

- [ ] 1.1 Add `encode_mu(x)` and `decode(z)` methods to `_Model` in `src/picoface/_internals/model_api.py`, each raising `NotImplementedError` by default (matching the existing `classify`/`sample` pattern), and a `_require_capability(model, "latent_access", ...)` guard usable by callers. Verify: a model whose `capabilities` frozenset omits `"latent_access"` raises the shared `CapabilityError` when the guard is used against it.
- [ ] 1.2 Declare `"latent_access"` in `_VAE.capabilities` (`src/picoface/_internals/generator_internals.py`) and implement `encode_mu(x)` (wrapping the existing `encode(x)[0]`) and `decode(z)` (wrapping `self.decoder(z)`). Verify: a unit test round-trips a batch of preprocessed images through `encode_mu` then `decode` and asserts the output shape matches `input_shape`, and that `decode(z)` for a random `z` produces the same shape as `sample(n)`.
- [ ] 1.3 Verify `_Autoencoder` and the CNN classifier model do not declare `"latent_access"` (no code change expected). Verify: a test asserts `"latent_access" not in model.capabilities` for both, and that the capability guard from 1.1 raises against each.

## 2. capstone-linkage: `classify_generated()`

- [ ] 2.1 Implement a taxonomy-check helper that compares `classifier_model.class_names` and `generator_model.class_names` as sets and raises a clear, named error (following the existing `ShapeError`-style convention) on mismatch. Verify: a test with a classifier and generator built from datasets with different class names raises that error before any generation or classification happens.
- [ ] 2.2 Implement per-class empirical latent cluster computation: for each class name in `data`, call `generator_model.encode_mu()` on that class's images and compute the mean and standard deviation of the resulting vectors. Verify: a unit test checks the returned per-class mean vector has `generator_model`'s latent dimensionality, and that two different classes in a separable stub dataset (`kind="shapes"`) produce distinguishably different means.
- [ ] 2.3 Implement class-targeted sampling: draw `n` latent vectors per class near that class's empirical cluster (mean + noise scaled by the spread from 2.2) and decode each via `generator_model.decode()`. Verify: a test checks the output is `n * num_classes` images in `generator_model`'s image shape and `uint8` dtype, matching `generate()`'s existing output convention.
- [ ] 2.4 Implement classification of the generated images with `classifier_model` and assemble the per-class and overall intended-vs-predicted agreement report. Verify: a unit test with a mocked or trivially separable classifier confirms the reported agreement rate matches a hand-computed value.
- [ ] 2.5 Wire up `classify_generated(classifier_model, generator_model, data, n)` in `src/picoface/linkage.py`, composing 2.1–2.4, and raising the capability error from `_Model`/`latent_access` (Task 1.1) when `generator_model` lacks the capability. Verify: an end-to-end test trains a CNN and a VAE on the stub dataset, calls `classify_generated()`, and checks the report's shape (one entry per class plus an overall figure) without asserting a specific accuracy value (matching how Phase 3c's diagnostics were recorded, not pinned).

## 3. capstone-linkage: `activation_maximize()`

- [ ] 3.1 Implement the gradient-ascent core: initialize from random noise in `[0, 1]`, run a fixed internal number of ascent steps at a fixed internal learning rate against `model.classify()`'s score for the target class index, clamping pixel values to `[0, 1]` after every step. Verify: a unit test checks the target class's score is higher after ascent than at the random-noise starting point.
- [ ] 3.2 Add input validation: raise the shared capability error if `model` lacks the `classify` capability, and a clear named error if `target_class` is not one of `model.class_names`. Verify: tests cover both error paths (an autoencoder model, and an unknown class name against a trained classifier).
- [ ] 3.3 Wire up `activation_maximize(model, target_class)` in `src/picoface/linkage.py`, returning a `uint8` image in the model's image shape (matching `generate()`'s output convention). Verify: a test calls it against both a trained CNN and a trained VAE from the stub dataset and checks output shape and dtype for each.

## 4. Integration and documentation

- [ ] 4.1 Add `tests/test_linkage.py` covering every scenario in `specs/capstone-linkage/spec.md` and `specs/model-interface/spec.md` (capability errors, taxonomy mismatch, per-class report shape, `activation_maximize` output shape/dtype, latent-access round-trip). Verify: `pytest tests/test_linkage.py` passes.
- [ ] 4.2 Write `src/picoface/linkage.py`'s module docstring and `__all__` to match the existing convention in `classifier.py`/`generator.py` (what the module is for, no internals leaked). Verify: docstring present, `__all__` lists exactly `classify_generated` and `activation_maximize` plus any shared error types re-exported.
- [ ] 4.3 Run the full test suite and confirm no regressions in the classifier or generator arms. Verify: `pytest` passes with no failures outside expected new tests.
