## 1. Real-dataset test fixture

- [x] 1.1 Add a way for tests to obtain a real Dataset Forge export on demand (e.g. a pytest fixture that runs the Forge's export with a fixed seed into a temp directory), without committing a generated dataset to the repo. Verify: a new test using the fixture loads a bundle via `load_dataset()` with the expected shape and 7 classes. Done: `tests/conftest.py`'s session-scoped `real_dataset` fixture exports the shipped default config (seed 0) into a `tmp_path_factory` directory; `tests/test_real_dataset.py::test_real_dataset_fixture_loads_expected_shape_and_classes` verifies shape (7000/1400, 28, 28, 1) and 7 classes.
- [x] 1.2 Identify and list the existing stub-based tests that validate model *quality* rather than plumbing (accuracy bounds, CPU time budget, `classify_generated()` agreement) and are in scope to gain a real-dataset counterpart per design.md's "stub stays, real dataset is added" decision. Verify: the list is recorded in this change's tasks/PR notes and matches the requirements touched in `specs/shape-classifier/spec.md` and `specs/shape-generator/spec.md`. Done: list recorded in `tests/test_real_dataset.py`'s module docstring (`test_training_wall_clock_under_generous_ceiling` -> shape-classifier CPU budget; `test_vae_training_wall_clock_under_generous_ceiling` -> shape-generator CPU budget; `test_classify_generated_reports_every_class_from_in_memory_models` -> capstone-linkage structural checks on real content); real-dataset counterparts added in the same file, all passing (4/4, ~31s).

## 2. Loss-balance tuning (log-variance floor, multiplier, KL schedule)

- [x] 2.1 Reproduce Phase 5's baseline numbers (CNN/VAE held-out accuracy, `classify_generated()` agreement, reconstruction agreement) on a fresh real-dataset export at current defaults, to confirm the starting point before changing anything. Verify: numbers fall within Phase 5's recorded ranges in `openspec/changes/archive/2026-09-23-picoface-phase5/diagnostics.md`. Done: seed-0 default export reproduces Phase 5's exact recorded numbers (CNN 0.974, VAE 0.954, `classify_generated()` 0.35, reconstruction 0.26) via `dataset_forge.smoke.smoke()`.
- [x] 2.2 Sweep the log-variance floor and the log-variance learning-rate multiplier (`LOG_VAR_FLOOR`, `LOG_VAR_LR_MULTIPLIER` in `generator_internals.py`) against the real dataset, recording reconstruction agreement, classification accuracy, and the learned log-variances' trajectory per epoch for each setting tried. Verify: results recorded in this change's diagnostics file (task 8.1) with at least 3 distinct settings compared. Done: 8 settings tried (floor in {-1,-2,-3,-6,-15}, multiplier in {2,3,10,30}); results recorded in diagnostics.md. Key finding: at the default floor (-6), `reconstruction_log_var` never reaches the floor in a 10-epoch run — it settles at -4.45 whether the floor is -6 or -15 (identical trajectories), so the floor does not bind and is not the bottleneck.
- [x] 2.3 Sweep the KL annealing schedule's length/shape (end point stays fixed at weight 1, per the unchanged `shape-generator` requirement). Verify: results recorded alongside 2.2, same metrics. Done: `KL_ANNEAL_FRACTION` in {0.1, 0.25, 0.5, 0.8, 1.0} tried; results recorded in diagnostics.md. No measurable effect on reconstruction agreement (0.279-0.293 across all values) or on which classes reconstruct.
- [x] 2.4 Pick the best-performing loss-balance setting found in 2.2–2.3 and set it as the new default in `generator_internals.py`. Verify: the full existing generator test suite (stub-based) still passes, and the real-dataset quality tests from task 1 run against the new default. Decision: **no change** — none of the 13 settings tried (8 floor/multiplier + 5 KL-fraction) beat the current defaults on both `classify_generated()` and reconstruction agreement simultaneously; differences are within single-seed/n=20 noise (~0.04 stderr). Non-round classes (triangle, star, smiley, negative_smiley) stayed at ~0.0 reconstruction agreement in every setting tried. Defaults (`LOG_VAR_FLOOR=-6.0`, `LOG_VAR_LR_MULTIPLIER=10`, `KL_ANNEAL_FRACTION=0.5`) are unchanged, so no code edit or re-run of task 1's tests was needed.

## 3. `latent_dim` retuning

- [x] 3.1 Sweep `latent_dim` around the current provisional value of 8, using the loss-balance setting chosen in 2.4, against the real dataset. Verify: results recorded in the diagnostics file with classification accuracy and reconstruction/`classify_generated()` agreement per value tried. Done: swept {4, 8, 16, 32, 64, 96, 128, 192} at seed 0, then confirmed 8 vs 64 vs 128 at seeds 1-2. Unlike loss-balance tuning, this one moves the needle: `classify_generated()`/reconstruction agreement roughly double between 8 and 64-128, with diminishing returns above 128; held-out VAE accuracy also improves (no trade-off). Full tables in diagnostics.md.
- [x] 3.2 Set the best-performing `latent_dim` as the new default if it improves on 2.4's result; otherwise keep 8. Verify: chosen value and rationale recorded in the diagnostics file. Done: `LATENT_DIM` set to 128 in `generator_internals.py` (3-seed mean `classify_generated()` 0.474 / reconstruction 0.503, vs 8's 0.357 / 0.314); rationale recorded in the constant's comment and diagnostics.md. Full test suite re-run: 200/200 passed.

## 4. Decision checkpoint: did tuning resolve the blob problem?

- [x] 4.1 Compare the best result from tasks 2–3 against Phase 5's baseline (`classify_generated()` 0.32–0.35, reconstruction agreement 0.26–0.31, only circle/ring recognizable) and record whether non-round classes (square, triangle, star, smiley, negative_smiley) are now visually/quantitatively recognizable in generated and reconstructed samples. Verify: a figure comparable to Phase 5's `generated.png`/`reconstructed.png` is produced (not committed, per the project's existing "generated artifacts aren't committed" convention) and described in the diagnostics file. Done: `generated.png`/`reconstructed.png` produced (not committed) via `dataset_forge.smoke.write_figures` at `latent_dim=128`. **Partial resolution**: square, triangle, ring, circle are now clearly recognizable (triangle's points and square's corners are visible in both generated and reconstructed samples, a qualitative change from pure blobs). negative_smiley's agreement numbers also rose substantially, but visually the CNN appears to key on its ring-plus-dark-fill polarity rather than its facial dots, which stay lost. star and smiley remain unresolved — still round blobs, facial features and star points are not reproduced. Full description in diagnostics.md.
- [x] 4.2 Per design.md's Decisions (tuning first, architecture only if tuning doesn't get there): if 4.1 shows the blobs persist, write an explicit "Open question for a follow-up change" section in the diagnostics file naming decoder capacity and label-conditioning as the untried next step, with the evidence for why tuning alone wasn't sufficient. Do not implement any decoder architecture change in this task. Verify: the section exists in the diagnostics file, or is explicitly absent because 4.1 found tuning sufficient. Done: section added to diagnostics.md (task 8.1) — star and smiley/negative_smiley's facial detail remain unresolved by loss-balance and latent-capacity tuning alone; decoder capacity and label-conditioning are named as the untried next step for a follow-up change. No decoder architecture change made in this task.

## 5. Secondary, independent knobs

- [x] 5.1 Try decoupled weight decay (`AdamW`, excluding the log-variance parameters) at a small value and compare classification/reconstruction against the task 2–3 default. Verify: recorded in the diagnostics file; adopted only if it doesn't regress either metric. Decision: **not adopted**. Swept weight_decay in {0, 1e-5, 1e-4, 1e-3} (AdamW on the non-log-var param group; identical to Adam at 0). 1e-5/1e-4 are noise-level vs. baseline; 1e-3 measurably regresses `classify_generated()` (0.543 -> 0.493). No value improves on the baseline, so no code change.
- [x] 5.2 Try a small change to the classification head's architecture (e.g. hidden-layer width) and compare against the current default. Verify: recorded in the diagnostics file; adopted only if it improves classification without regressing reconstruction. Decision: **not adopted**. Swept `_HEAD_HIDDEN_SIZE` in {16, 32, 64, 128} across 3 seeds; 128 improves the 3-seed mean on both metrics but is not a consistent per-seed win (one seed regresses `classify_generated()` while improving reconstruction) — a noise-level, inconsistent signal like the loss-balance knobs, unlike `latent_dim`'s unambiguous cross-seed win. Kept at 32.
- [x] 5.3 Compare Dataset Forge's default of 1,000 images/class against 2,000/class using the tuned model from tasks 2–4, confirming Phase 5's finding (extra data helps classification, not the decoder) still holds post-tuning. Verify: recorded in the diagnostics file; if adopted, update `dataset_forge/configs/default.json` and its default-count documentation. Decision: **adopted, with an update to Phase 5's finding**. At `latent_dim=128`, 2,000/class measurably improves reconstruction agreement too (not just classification), confirmed at 2 seeds (seed 0: 0.51 -> 0.60; seed 1: 0.46 -> 0.66), unlike Phase 5's original finding under the `latent_dim=8` bottleneck ("more data does not help generation at all"). `star`/`smiley` are still weak but no longer exactly 0. Training time stays well within budget (~31s combined). `dataset_forge/configs/default.json`'s `train_per_class` changed from 1000 to 2000; `tests/test_real_dataset.py`'s shape assertion updated to match (14000 train images); full suite re-run, 200/200 passed.

## 6. `train()` defaults and CPU time-budget verification

- [x] 6.1 Run `train()` with its actual default `epochs=10, batch_size=16, learning_rate=1e-3` end-to-end against a real Dataset Forge export (not a separate script) for both `build_classifier()` and `build_vae()`, and record held-out classification accuracy. Verify: accuracy is high enough to be a believable capstone judge (compare against Phase 5's 0.94–0.97 range); if not, note candidate alternate defaults with their measured effect. Done: called `picoface.classifier.train`/`picoface.generator.train` directly (also exercised via `tests/test_real_dataset.py`) at true defaults on the (now 2000/class) real export, 3 seeds: CNN accuracy 0.979-0.991, VAE accuracy 0.953-0.999 — comfortably above Phase 5's 0.94-0.97 range. No alternate default needed.
- [x] 6.2 Measure wall-clock training time for 6.1 on this session's machine and confirm it stays within "seconds to minutes," consistent with Phase 5's smoke-check numbers. Verify: time recorded in the diagnostics file. Done: CNN ~9.5s, VAE ~20.8s (10 epochs each, 2000/class), well within the 5-minute budget and consistent with Phase 5's per-step-scaling observation.
- [x] 6.3 If 6.1 concludes a different default is warranted, update `train()`'s default in the shared model-interface internals, then update the `model-interface` and `shape-classifier` specs to match via `opsx:update` before this change archives (proposal.md, Modified Capabilities). If no change is warranted, record that conclusion in the diagnostics file and leave the specs as-is. Verify: spec text and shipped default agree. Done: no different default is warranted (6.1); `train()`'s defaults are unchanged, and `model-interface`/`shape-classifier` specs are left as-is — no `opsx:update` needed. This change's own spec deltas (the CPU time-budget wording covering the real dataset) already match the shipped, unchanged defaults.

## 7. Capstone linkage retuning

- [x] 7.1 Re-run `classify_generated()`'s existing sampling approach (mean plus per-dimension spread of `mu`) against the tuned model from tasks 2–4 and record whether agreement improved. Verify: recorded in the diagnostics file, compared against Phase 5's 0.32–0.35 baseline. Done: at `latent_dim=128` + 2000/class, `classify_generated()` overall lands around 0.45-0.54 across seeds — a clear improvement on Phase 5's 0.32-0.35.
- [x] 7.2 If 7.1 shows room to improve, try a small adjustment to the sampling spread (e.g. a scale factor on the per-dimension spread) and record the effect. Verify: recorded in the diagnostics file; adopted only if it improves agreement without a named downside. Done: swept a scale factor on the per-dimension spread (0.25-2.0), reseeding the sampling RNG identically per scale for a fair comparison (an initial unseeded pass was discarded as invalid - see diagnostics.md). 0.6-0.8 consistently beat 1.0 across 6 sampling seeds (mean ~0.52 vs ~0.47) with no downside found; adopted `CLUSTER_SAMPLE_SPREAD = 0.75` in `linkage_internals.py`.
- [x] 7.3 Re-run `activation_maximize()` against the tuned model and assess whether its output is recognizable or still adversarial noise; if noise, try adding a simple regularization term (e.g. L2 or blur penalty on the ascended image) and/or adjusting step count/learning rate. Verify: before/after images described in the diagnostics file (not committed); adopted only if the change measurably improves visual recognizability without breaking the existing `capstone-linkage` tests. Done: unregularized ascent is high-frequency adversarial noise for most classes (only square/circle show a rough blob). Tried TV regularization (too weak at reasonable weights) and periodic Gaussian blur (every 10 steps, sigma 1.0): visibly more shape-like (star in particular becomes a recognizable pointed star), target-class logit still rises substantially from its random start (roughly half the unregularized run's inflated score, which is itself mostly adversarial). Adopted `ASCENT_BLUR_EVERY=10`, `ASCENT_BLUR_SIGMA=1.0` in `linkage_internals.py`. Full `capstone-linkage` test suite still passes (200/200 overall).

## 8. Diagnostics record and project documentation

- [x] 8.1 Write `diagnostics.md` in this change's directory, following Phase 5's format (setup, baselines, per-step results, decision, Phase-7-facing inputs), consolidating the results from tasks 2–7. Verify: file exists and every numeric claim in it traces to a task above. Done: `openspec/changes/picoface-phase6/diagnostics.md` written, consolidating every sweep table from tasks 2–7 plus a final 3-seed validation run and a Phase-7-facing inputs section.
- [x] 8.2 Update `openspec/ROADMAP.md`: mark Phase 6 done with a link to this change's archive location (once archived), and resolve or re-scope each of its currently-listed Phase 6 open items (log-variance floor/multiplier, KL schedule, `latent_dim`, weight decay, classification head architecture, label-conditioned decoder feasibility, `activation_maximize()` regularization/steps, `classify_generated()` sampling, concrete accuracy/quality bar) to point at what this phase resolved versus what it explicitly deferred (task 4.2's open question, if any; the quality bar deferred to Phase 7 per this change's scoping). Verify: no Phase 6 line in the ROADMAP's Open Questions section still reads as unaddressed by this change. Done: Phase Plan table, Phase 6's descriptive section, Risks/Trade-offs, and Open Questions all updated; also fixed Phase 5's stale "Not started" status (it was archived but the table was never updated). New open item added for the `star`/`smiley` follow-up this phase surfaced.
- [x] 8.3 Run the full existing test suite (stub-based and any real-dataset tests added in task 1) and confirm it passes. Verify: `pytest` exits 0 across `src/picoface` and `dataset_forge`.

## 9. Change hygiene

- [x] 9.1 Confirm no generated dataset exports, figures, or diagnostics-run artifacts are committed (per the project's existing "generated datasets/figures are never committed" convention) — only code, config defaults, and the written-up `diagnostics.md`. Verify: `git status`/diff for this change contains no files under a Dataset Forge output directory. Done: `git status` shows only source/spec files changed plus `diagnostics.md`, `tests/conftest.py`, `tests/test_real_dataset.py`; all sweep exports and figures were written under `/tmp` or the pre-existing, gitignored `dataset_forge/output/`, never staged.
- [x] 9.2 Run `openspec validate --change "picoface-phase6" --strict` and resolve any reported issues before implementation is considered apply-ready. Verify: command exits clean. Done: `openspec validate picoface-phase6 --strict` -> "Change 'picoface-phase6' is valid".

## 10. Decoder capacity escalation (user-directed, before archiving)

After task 8 completed, the user reviewed task 4.2's open question (`star`/`smiley` unresolved by loss-balance/`latent_dim` tuning) and explicitly asked to push further on decoder capacity before archiving — the escalation path task 4.2 and design.md's Non-Goals had named but deliberately not built. This reopens that non-goal for this same change rather than a follow-up one, at the user's direction.

- [x] 10.1 Sweep a decoder-only intermediate channel width (independent of the encoder trunk) against the real dataset, isolating `star`/`smiley`. Verify: recorded in diagnostics.md. Done: first single-seed pass was too noisy to trust (the "baseline" re-run swung from 0.10-0.25 reconstruction agreement, matching task 4's numbers, to 0.70 on a different RNG draw of the *same* architecture) — different decoder widths consume different amounts of RNG state during parameter initialization before training's batch-shuffling even starts, confounding single-seed comparisons more than for the scalar hyperparameters swept in tasks 2-3. Redone as a 3-seed comparison (mid-channels 8/64/128) with matched seeding per seed and `n=50` (not 20) evaluation samples to cut per-class standard error.
- [x] 10.2 Confirm the result holds and check for further room by testing wider values. Verify: recorded in diagnostics.md. Done: mid=64/128 both showed large, consistent, 3-seed-robust improvement over mid=8 on `star`/`smiley` reconstruction agreement specifically, with no regression on the other five classes; mid=192/256 plateaued at essentially the same numbers as mid=128 (diminishing returns), the same pattern `latent_dim` showed in task 3.
- [x] 10.3 Adopt the chosen width in `generator_internals.py`; re-run the full test suite; re-run the final combined validation. Verify: full suite passes; final numbers recorded. Done: `_DECODER_MID_CHANNELS = 128` (new, decoder-only constant; the encoder trunk's channel counts are untouched). Full suite: 200/200 passed. Final 3-seed validation via `dataset_forge.smoke.smoke()` (2000/class, all Phase 6 changes combined): `classify_generated()` overall 0.643-0.686 (up from 0.46-0.59 before this escalation; Phase 5's baseline was 0.32-0.35), reconstruction overall 0.814-0.850 (up from 0.60-0.66; Phase 5's baseline was 0.26-0.31). `smiley` is now essentially resolved (reconstruction 0.45-0.70, `classify_generated()` 0.20-0.85). `star`'s reconstruction is now solid (0.50-0.60, up from ~0.0-0.25) but its `classify_generated()` stays weak (0.00-0.25) even with the wider decoder — a distinct, still-open issue in how noisily `star`'s latent cluster is sampled (see diagnostics.md), not decoder capacity.
- [x] 10.4 Update `proposal.md`, `design.md`, and `diagnostics.md` to reflect that a decoder-capacity change was made in this phase (reversing the original "does not change decoder architecture" scoping), and narrow the task 4.2 open question to what's actually still open. Verify: `openspec validate picoface-phase6 --strict` passes after the edits. Done: `proposal.md` (What Changes, Modified Capabilities, Impact), `design.md` (Non-Goals, Decisions), `diagnostics.md` (new Decoder capacity escalation section, updated Phase 7 inputs and Figures), and `openspec/ROADMAP.md` (Phase 6 section, Risks, Open Questions) all updated. `openspec validate picoface-phase6 --strict` -> "Change 'picoface-phase6' is valid". Full suite re-run: 200/200 passed.
